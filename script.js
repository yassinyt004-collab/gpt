const toggle=document.querySelector('.menu-toggle');const nav=document.querySelector('.main-nav');if(toggle&&nav){toggle.addEventListener('click',()=>{const open=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',String(open));});}document.querySelectorAll('.main-nav a').forEach(a=>a.addEventListener('click',()=>nav?.classList.remove('open')));const io=new IntersectionObserver(e=>e.forEach(x=>{if(x.isIntersecting)x.target.classList.add('show')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));document.querySelectorAll('a[href$=".html"],a[href="menu.html"],a[href="index.html"]').forEach(link=>{link.addEventListener('click',e=>{const href=link.getAttribute('href');if(!href||link.target==='_blank'||href.startsWith('#')||href.startsWith('tel:'))return;e.preventDefault();document.body.classList.add('page-exit');setTimeout(()=>location.href=href,180);});});const filters=document.getElementById('filters');const grid=document.getElementById('menuGrid');function renderMenu(category='Tout'){if(!grid||!filters||typeof menuData==='undefined')return;grid.innerHTML='';const groups=category==='Tout'?menuData:menuData.filter(g=>g.category===category);groups.forEach(group=>group.items.forEach(item=>{const[name,desc,price,image]=item;const card=document.createElement('article');card.className='menu-card';card.innerHTML=`<span class="cat">${group.category}</span><img src="${image}" alt="${name}" loading="lazy"><div class="body"><h3>${name}</h3><p>${desc}</p><div class="price"><span>${price}</span><a class="mini-order" href="tel:0473905738">Commander</a></div></div>`;grid.appendChild(card)}));}function initFilters(){if(!filters||typeof menuData==='undefined')return;const cats=['Tout',...menuData.map(g=>g.category)];filters.innerHTML='';cats.forEach((cat,i)=>{const b=document.createElement('button');b.type='button';b.textContent=cat;if(i===0)b.classList.add('active');b.addEventListener('click',()=>{document.querySelectorAll('.filters button').forEach(x=>x.classList.remove('active'));b.classList.add('active');renderMenu(cat)});filters.appendChild(b)});renderMenu()}initFilters();