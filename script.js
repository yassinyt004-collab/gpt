const grid=document.getElementById('menuGrid'), tabs=document.getElementById('tabs'), search=document.getElementById('search');let active='all';
function norm(s){return s.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'')}
function renderTabs(){tabs.innerHTML=`<button class="tab active" data-id="all">Tout</button>`+menuCategories.map(c=>`<button class="tab" data-id="${c.id}">${c.title}</button>`).join('');tabs.onclick=e=>{if(!e.target.matches('.tab'))return;active=e.target.dataset.id;document.querySelectorAll('.tab').forEach(b=>b.classList.toggle('active',b.dataset.id===active));render()}}
function render(){const q=norm(search.value||'');let html='';menuCategories.forEach(cat=>{if(active!=='all'&&active!==cat.id)return;cat.items.forEach(([name,desc,price,img])=>{const txt=norm(cat.title+' '+name+' '+desc+' '+price);if(q&&!txt.includes(q))return;html+=`<article class="card"><img src="${img}" alt="${name}" loading="lazy" onerror="this.src='assets/hero-food.png'"><div class="content"><p class="eyebrow">${cat.title}</p><h3>${name}</h3><p>${desc}</p><div class="price"><span>${price}</span><a class="order" href="tel:0473905738">Commander</a></div></div></article>`})});grid.innerHTML=html||'<p>Aucun résultat.</p>'}
search.addEventListener('input',render);document.getElementById('hamb').onclick=()=>document.querySelector('nav').classList.toggle('open');renderTabs();render();
