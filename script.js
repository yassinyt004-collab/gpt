const grid=document.getElementById('menuGrid');const tabs=document.getElementById('tabs');const search=document.getElementById('search');let active='Tous';const categories=['Tous',...new Set(MENU_ITEMS.map(i=>i.category))];function renderTabs(){tabs.innerHTML=categories.map(c=>`<button class="tab ${c===active?'active':''}" data-cat="${c}">${c}</button>`).join('');document.querySelectorAll('.tab').forEach(b=>b.onclick=()=>{active=b.dataset.cat;renderTabs();renderCards();});}function renderCards(){const q=(search.value||'').toLowerCase();const data=MENU_ITEMS.filter(i=>(active==='Tous'||i.category===active)&&(`${i.name} ${i.category} ${i.description}`.toLowerCase().includes(q)));grid.innerHTML=data.map(i=>`<article class="card"><div class="imgWrap"><img src="${i.image}" alt="${i.name}" loading="lazy"></div><div class="cardBody"><span class="cat">${i.category}</span><h3>${i.name}</h3><p>${i.description}</p><div class="priceRow"><b class="price">${i.price}</b><a class="smallBtn" href="tel:0473905738">Commander</a></div></div></article>`).join('');}search.addEventListener('input',renderCards);document.getElementById('burgerBtn').onclick=()=>document.getElementById('navLinks').classList.toggle('open');renderTabs();renderCards();